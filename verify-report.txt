asar_size=43213783
top_level:
game
main.cjs
package.json
game_file_count=263
--- game-build ---
game-build" content="20260911-14"
--- main.cjs ---
const { app, BrowserWindow, Menu, shell } = require("electron");
const path = require("node:path");
const { fileURLToPath } = require("node:url");

const GAME_ENTRY = path.join(__dirname, "game", "index.html");
const GAME_ROOT = path.dirname(GAME_ENTRY);
let mainWindow = null;

function isGameFile(url) {
  try {
    const target = fileURLToPath(url);
    const relative = path.relative(GAME_ROOT, target);
    return relative === "" || (!relative.startsWith("..") && !path.isAbsolute(relative));
  } catch (_) {
    return false;
  }
}

function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1280,
    height: 720,
    minWidth: 960,
    minHeight: 540,
    show: false,
    autoHideMenuBar: true,
    backgroundColor: "#08070a",
    title: "魅魔杀",
    webPreferences: {
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: true,
      webSecurity: true,
    },
  });

  mainWindow.setAspectRatio(16 / 9);
  mainWindow.webContents.setWindowOpenHandler(({ url }) => {
    if (/^https?:/i.test(url)) shell.openExternal(url);
    return { action: "deny" };
  });
  mainWindow.webContents.on("will-navigate", event => {
    if (isGameFile(event.url)) return;
    event.preventDefault();
    if (/^https?:/i.test(event.url)) shell.openExternal(event.url);
  });
  mainWindow.webContents.on("before-input-event", (event, input) => {
    if (input.type !== "keyDown" || input.key !== "F11") return;
    event.preventDefault();
    mainWindow.setFullScreen(!mainWindow.isFullScreen());
  });
  mainWindow.once("ready-to-show", () => mainWindow.show());
  mainWindow.loadFile(GAME_ENTRY);
}

app.whenReady().then(() => {
  Menu.setApplicationMenu(null);
  createWindow();
});

app.on("window-all-closed", () => app.quit());
--- package.json ---
{
  "name": "succubus-kill",
  "version": "1.0.0",
  "main": "main.cjs"
}
--- key files ---
OK ext/game/index.html 1940
OK ext/game/assets/generated/angelica-level-10-special.1f484c88.webp 98548
OK ext/game/assets/generated/cards/skill-art-crimson-rampage.5202f136.webp 238760
OK ext/game/assets/generated/cards/skill-art-shadow-dance.eee03e1e.webp 192420
--- angelica skills in bundle ---
猩红暴走
ext/game/bundles/battle-skills.min.js:力量爆发
--- test battle button ---
data-open-modal="testBattle"
--- combatRoleText floating ---
狂战×
